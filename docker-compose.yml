version: '3.9'

services:
  redis:
    image: "redis:alpine"
    container_name: "redis-cache"
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    command: ["redis-server", "--appendonly", "yes"]


volumes:
  redis-data:
    driver: local
